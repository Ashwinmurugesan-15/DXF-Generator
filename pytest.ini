[pytest]
testpaths = tests
python_files = test_*.py
; asyncio_mode is only needed if pytest-asyncio is installed
; and the version expects it. If it causes warnings, it can be removed.
; asyncio_mode = auto
